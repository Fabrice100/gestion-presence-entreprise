# Configuration pytest pour le projet
# Tests settings et fixtures communes

import pytest
import django
from django.conf import settings
from django.test.utils import get_runner


def pytest_configure():
    """Configuration de pytest pour Django."""
    settings.configure(
        DEBUG=True,
        DATABASES={
            'default': {
                'ENGINE': 'django.db.backends.sqlite3',
                'NAME': ':memory:',
            }
        },
        SECRET_KEY='test-secret-key-for-testing-only',
        USE_I18N=True,
        USE_TZ=True,
        INSTALLED_APPS=[
            'django.contrib.admin',
            'django.contrib.auth',
            'django.contrib.contenttypes',
            'django.contrib.sessions',
            'django.contrib.messages',
            'django.contrib.staticfiles',
            'crispy_forms',
            'crispy_bootstrap5',
            'common',
            'accounts',
            'attendance',
            'leave',
            'reports',
            'tests',
        ],
        MIDDLEWARE=[
            'django.middleware.security.SecurityMiddleware',
            'django.contrib.sessions.middleware.SessionMiddleware',
            'django.middleware.common.CommonMiddleware',
            'django.middleware.csrf.CsrfViewMiddleware',
            'django.contrib.auth.middleware.AuthenticationMiddleware',
            'django.contrib.messages.middleware.MessageMiddleware',
            'django.middleware.clickjacking.XFrameOptionsMiddleware',
        ],
        ROOT_URLCONF='attendance_system.urls',
        AUTHENTICATION_BACKENDS=[
            'accounts.auth_backend.EmployeeIDBackend',
            'django.contrib.auth.backends.ModelBackend',
        ],
        CRISPY_ALLOWED_TEMPLATE_PACKS = "bootstrap5",
        CRISPY_TEMPLATE_PACK = "bootstrap5",
    )
    django.setup()


@pytest.fixture
def django_db_setup():
    """Configuration de la base de données pour les tests."""
    pass


@pytest.fixture(scope='session')
def django_db_keepdb():
    """Garde la base de données entre les tests pour les performances."""
    return True